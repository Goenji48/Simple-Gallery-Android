<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleGallery" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="colorSurface">@color/black</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="colorOnSurfaceVariant">@color/white</item>
        <item name="colorSurfaceContainer">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:background">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Toolbar" parent="Widget.Material3.Toolbar.OnSurface" />

    <style name="DefaultAlertDialogStyle" parent="Theme.Material3.Dark.Dialog.Alert">
        <item name="colorSurfaceContainerHigh">@color/black</item>
        <item name="colorPrimary">@color/white</item>
        <item name="scrimBackground">@color/white</item>
    </style>

</resources>